[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
clap = { version = "4", features = ["derive"] }
chrono = "0.4"
schemars = "1.0"
serde_json = "1"
ark = { path = "..", features = ["schemars"] }
